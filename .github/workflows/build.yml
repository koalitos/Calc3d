name: Build Multi-Platform

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: 🎉 Calc 3D Print ${{ github.ref_name }} - Release Inicial
          body: |
            # 🎉 Calc 3D Print ${{ github.ref_name }} - Release Inicial
            
            Sistema completo para calcular custos e preços de venda de impressões 3D.
            
            ## ✨ Destaques
            
            - 💰 **Cálculo Automático de Custos** - Filamento, energia, depreciação, embalagem e taxas
            - 📊 **Gestão Completa** - Máquinas, filamentos, projetos, vendas e despesas
            - 🎯 **Upload de G-code** - Extração automática de peso e tempo de impressão
            - 💾 **Backup Integrado** - Seus dados sempre seguros
            - 🔐 **Sistema de Login** - Múltiplos usuários com autenticação JWT
            - 🎨 **Interface Moderna** - Design profissional com tema escuro
            - 🔄 **Auto-updater** - Atualizações automáticas
            
            ## 📥 Instalação
            
            ### 🪟 Windows
            Baixe e execute `Calc3DPrint-Setup-1.0.0.exe`
            
            ### 🍎 macOS
            - **Intel**: `Calc3DPrint-1.0.0-x64.dmg`
            - **Apple Silicon (M1/M2/M3)**: `Calc3DPrint-1.0.0-arm64.dmg`
            
            ### 🐧 Linux
            - **Universal**: `Calc3DPrint-1.0.0-x64.AppImage`
            - **Debian/Ubuntu**: `calc3dprint_1.0.0_amd64.deb`
            
            **Requisitos**: 4 GB RAM, 500 MB espaço
            
            ## 🚀 Novidades
            
            ✅ Interface moderna e intuitiva  
            ✅ Tela de loading animada  
            ✅ Ícone personalizado (impressora 3D)  
            ✅ Backend integrado (sem instalações extras)  
            ✅ Banco de dados SQLite local  
            ✅ Instaladores profissionais para todas as plataformas  
            ✅ Auto-updater (atualizações automáticas)  
            ✅ Suporte multiplataforma (Windows, macOS, Linux)  
            
            ## 🐛 Reportar Bugs
            
            Encontrou um problema? [Abra uma issue](https://github.com/koalitos/calc3D/issues)
            
            ---
            
            **Desenvolvido com ❤️ para a comunidade de impressão 3D**
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    needs: create-release
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
      
      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          cd backend && npm install --legacy-peer-deps && cd ..
          cd frontend && npm install --legacy-peer-deps && cd ..
      
      - name: Build frontend
        run: npm run build
        env:
          CI: false
      
      - name: Build Windows installer
        run: npm run dist:win
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
      
      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: |
            dist/*.exe
          retention-days: 5
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    needs: create-release
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
      
      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          cd backend && npm install --legacy-peer-deps && cd ..
          cd frontend && npm install --legacy-peer-deps && cd ..
      
      - name: Build frontend
        run: npm run build
        env:
          CI: false
      
      - name: Build macOS DMG
        run: npm run dist:mac
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
      
      - name: List generated files
        run: ls -lah dist/
      
      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: |
            dist/*.dmg
          retention-days: 5
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    needs: create-release
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
      
      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          cd backend && npm install --legacy-peer-deps && cd ..
          cd frontend && npm install --legacy-peer-deps && cd ..
      
      - name: Build frontend
        run: npm run build
        env:
          CI: false
      
      - name: Build Linux packages
        run: npm run dist:linux
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
      
      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-packages
          path: |
            dist/*.AppImage
            dist/*.deb
          retention-days: 5
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/*.AppImage
            dist/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
